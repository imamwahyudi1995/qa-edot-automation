[pytest]
addopts = -n auto --maxfail=3 --dist=loadscope

# -n auto: run tests in parallel using all available CPU cores
# --maxfail=3: stop after 3 failures
# --dist=loadscope: better distribution for class/module scoped fixtures 